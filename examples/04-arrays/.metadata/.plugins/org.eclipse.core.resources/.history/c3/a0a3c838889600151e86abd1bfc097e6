
public class FullExample {

	public static void main(String[] args) {
		// Деклариране на едномерен масив.
		int[] array1;
		
		// Инизиализиране на едномерен масив с дължина от 7 елеменета. Първоначално пълен с нули.
		array1 = new int[7];
		
		// Добавяне на стойност в масив. Попълване на 3-тия елемент с индекс 2.
		array1[2] = 6;
		
		// Обхождане на масива и извеждане на всички стойности на конзолата/екрана.
		for (int i = 0; i < array1.length; i++) {
			System.out.println(array1[i]);
		}
		
		// Деклариране и инизиализиране на едномерен масив. Първоначално пълен с нули.
		int[] array2 = new int[7];
		
		// Деклариране и инизиализиране на едномерен масив директно със стойностти.
		int[] array3 = {55, 44, 33, 22, 11};

	}

}
