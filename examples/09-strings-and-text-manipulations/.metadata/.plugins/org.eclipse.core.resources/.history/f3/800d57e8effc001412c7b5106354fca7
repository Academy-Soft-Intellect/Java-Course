package comparison;

public class StringEquals {

	public static void main(String[] args) {
		
		String first = "�?з Програмирам на Java";
		
		String second = "�?з" + " Програмирам на " + "Java";
		
		boolean refComparison = first == second;
		
		System.out.format("Сравн�?ване на референциите �? ==: %s \n", refComparison);
		
		boolean valueComparison = first.equals(second);
		
		System.out.format("Сравн�?ване на �?трингове �? equals: %s", valueComparison);

	}
}
